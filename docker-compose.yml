version: '3.8'

services:
  app:
    build: .
    container_name: linktocompany
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - JWT_SECRET_KEY=linktocompany-production-secret-key
    volumes:
      - ./data:/app/backend/data
    restart: unless-stopped
